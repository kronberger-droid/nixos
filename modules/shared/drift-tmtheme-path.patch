diff --git a/src/highlight.rs b/src/highlight.rs
index 670d1eb..9314c8c 100644
--- a/src/highlight.rs
+++ b/src/highlight.rs
@@ -19,18 +19,28 @@ pub struct Highlighter {
 impl Highlighter {
     pub fn new(theme_name: &str, enabled: bool) -> Self {
         let syntaxes = two_face::syntax::extra_newlines();
+        // A `theme` naming a .tmTheme file is loaded from disk. The built-in
+        // palettes and syntect's bundled set are both fixed at compile time,
+        // so this is the only way to drive highlighting from a palette the
+        // binary has never heard of -- a base16 scheme generated by the
+        // user's dotfiles, say. Falls back like any other unknown name.
+        let load_fallback = || {
+            let b = builtin_named("onedark").expect("onedark palette exists");
+            builtin_theme("onedark", &b)
+        };
         // The `ansi` palette holds ANSI color names (not hex) and is meant to
         // run with syntax highlighting off, so it never builds a syntect theme.
-        let theme = match builtin_named(theme_name) {
-            Some(b) if theme_name != "ansi" => builtin_theme(theme_name, &b),
-            _ => ThemeSet::load_defaults()
-                .themes
-                .get(theme_name)
-                .cloned()
-                .unwrap_or_else(|| {
-                    let b = builtin_named("onedark").expect("onedark palette exists");
-                    builtin_theme("onedark", &b)
-                }),
+        let theme = if theme_name.ends_with(".tmTheme") {
+            ThemeSet::get_theme(theme_name).unwrap_or_else(|_| load_fallback())
+        } else {
+            match builtin_named(theme_name) {
+                Some(b) if theme_name != "ansi" => builtin_theme(theme_name, &b),
+                _ => ThemeSet::load_defaults()
+                    .themes
+                    .get(theme_name)
+                    .cloned()
+                    .unwrap_or_else(load_fallback),
+            }
         };
         Highlighter {
             syntaxes,
